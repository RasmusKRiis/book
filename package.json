{
  "name": "my-netlify-site",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build step' && exit 0",
    "validate": "node scripts/validateBooks.js"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "ajv": "^8.12.0"
  }
}
